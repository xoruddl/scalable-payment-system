apiVersion: 1

providers:
  - name: remittance
    type: file
    disableDeletion: false
    # 화면에서 고친 것이 파일로 돌아오지는 않는다. 대시보드의 정본은 저장소다.
    allowUiUpdates: true
    options:
      path: /var/lib/grafana/dashboards
      foldersFromFilesStructure: false
