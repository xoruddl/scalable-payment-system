# AGENTS.md

이 저장소에서 작업하는 AI 에이전트를 위한 안내 문서다. **작업 시작 전에 이 문서부터 읽는다.**

## 이 프로젝트가 무엇인가

송금/결제 시스템을 MSA로 구현하는 **학습용 프로젝트**다.

목표는 완성도 높은 결제 제품이 아니라, **대규모 분산 시스템에서 실제로 터지는 문제
(정합성 깨짐, 과부하, 장애 전파, 확장)를 재현하고 해결하는 것**이다. 송금 도메인을 고른 이유도
부분 실패가 곧 사고가 되는 특성 때문에 멱등성·분산락·Saga·Outbox를 안 쓸 수 없어서다.

따라서 작업을 제안할 때는 **도메인 기능을 풍부하게 만드는 것보다 현재 Phase가 목표로 하는
분산 시스템 패턴을 제대로 연습하는 쪽을 우선**한다.

**도메인 깊이보다 서비스 수를 늘리는 쪽에 관심이 있다.** 확장하면서 생기는 문제
(서비스 디스커버리, 크로스 서비스 관측, 트래픽 정책)는 도메인이 풍부해져서 생기는 복잡도와
종류가 다르다. 뒷 Phase를 계획할 때 기존 서비스에 엔드포인트를 더하는 대신 **새 서비스를 낼
자리가 있으면 짚는다.** 다만 도메인은 지금처럼 최소로 두고, 특정 패턴이 요구할 때만 넓힌다.

### 최종 목표는 "현업에서 쓰는 구조"이고, 그 이유를 말할 수 있어야 한다

이 저장소는 **면접 자료로도 쓴다.** 돌아가는 코드만으로는 부족하고 **선택의 근거**가 함께 남아야 한다.
기준 스택은 **토스 / 우아한형제들(배민)**이다.

> ⚠️ **"토스가 쓰니까"는 근거가 아니다.** 기술 선택을 적을 때는 반드시
> **문제 → 대안 비교 → 선택 이유 → 포기한 것 → 검증 방법** 순서로 적는다.
> 특히 **포기한 것**을 비워두지 않는다 — 없으면 "장점만 읽고 도입했다"가 된다.
> 두 회사의 내부 스택을 **출처 없이 단정하지 않는다.**

연습이 목적이라고 해서 **자체 구현이 최종 형태는 아니다.** 이 저장소는 두 단계로 간다.

```
① 손으로 만들어본다   왜 필요한지 알기 위해   (Redisson 대신 SET NX PX, CDC 대신 폴링 릴레이)
② 현업 기술로 갈아탄다  배울 걸 다 배웠으면      (Redisson, Debezium, Flyway, …)
```

**②를 잊지 않는다.** 자체 구현이 잘 돌아가고 테스트가 통과한다는 이유로 끝났다고 보면 안 된다.
무엇이 아직 자체 구현이고 무엇으로 바꿀 예정인지는 `docs/DECISIONS.md`에 있다.

> **고칠지 말지는 측정이 정하고, 무엇으로 고칠지는 현업 표준이 정한다.**
> 측정이 아프다고 말하지 않는 곳은 건드리지 않는다. 문제가 진짜라면 자체 구현을 새로
> 만들지 말고 표준 도구(Redisson, ShedLock, Debezium, Spring Security, Temporal …)를
> 이름으로 대고 그 대가를 함께 적는다.

## 문서 지도

| 문서 | 역할 | 언제 읽나 |
|---|---|---|
| `AGENTS.md` (이 문서) | 저장소 진입점, 작업 규칙 | 작업 시작 전 항상 |
| `docs/ARCHITECTURE.md` | **구조와 흐름** — 서비스 경계, 이벤트가 흐르는 길, 정합성 장치 | **코드를 건드리기 전에** |
| `docs/CODE_READING_GUIDE.md` | **코드 읽기와 면접 준비** — 읽을 메서드, 관련 테스트, 설명할 질문 | 코드를 공부하거나 면접을 준비할 때 |
| `docs/DECISIONS.md` | **기술 결정 기록** — 자체 구현을 무엇으로 왜 갈아탔나 | 라이브러리·엔진을 도입할 때 |
| `docs/ROADMAP.md` | **앞으로 할 일** — Phase 0~13 계획과 체크박스 | 다음에 뭘 할지 정할 때 |
| `docs/PROGRESS.md` | **실제로 한 일과 그 이유** — 시간순 기록 | 현재 상태를 파악할 때 (문서 상단에 현재 위치·브랜치·빌드 상태) |
| `docs/CONTRIBUTING.md` | **규칙** — 커밋 컨벤션, git flow 브랜치 전략, PR 본문 작성, 주석 스타일 | 커밋/브랜치/PR을 만들기 전, 주석을 쓰기 전 |
| ~~`docs/openapi.yaml`~~ | **없앴다** (Phase 4). API 계약은 각 서비스가 `/v3/api-docs`로 스스로 낸다 — 손으로 쓴 문서는 이미 코드와 어긋나 있었다. 공개용은 `/v3/api-docs/public`, 내부용은 `/v3/api-docs/internal` | 엔드포인트를 바꿀 때 **문서를 고칠 필요가 없다** |
| `docs/SLO.md` | **약속한 목표와 측정된 용량** — 접수/종결 지연·오류율, 지금 용량은 **40 TPS** | 성능을 논하기 전에. **처리량 숫자 하나만으로 좋아졌다고 말하지 않는다** |
| `docs/HOMELAB.md` | **측정 전용 홈서버** — 성능 숫자는 여기서 잰다 | 부하 테스트·baseline을 잴 때 |
| `docs/HELP.md` | Spring Initializr 자동 생성물 (`.gitignore` 대상) | 볼 일 없음 |

문서는 루트의 이 파일 하나만 남기고 나머지는 `docs/`에 모아두었다.
(`AGENTS.md`는 에이전트 도구가 저장소 루트에서 찾는 규약이라 옮기지 않는다.)

문서마다 관심사가 다르다 — **구조(ARCHITECTURE) / 결정(DECISIONS) / 계획(ROADMAP) /
기록(PROGRESS) / 규칙(CONTRIBUTING)**. 내용을 중복해서 적지 말고 각자 자리에 남긴다.

- `ARCHITECTURE.md`는 **지금 이렇게 생겼다**를 쓴다. 겪은 문제의 서술은 `PROGRESS.md` 몫이고,
  여기엔 그 결과 지켜야 할 것만 남긴다.

> **이 문서가 커지면 주제별로 쪼개도 된다** — 미리 물어볼 필요 없다.
> 다만 `AGENTS.md`는 루트에 남기고 내용만 `docs/`로 옮긴 뒤 링크로 잇는다.
> **프로젝트 목적 / 작업 규칙 / 문서 지도는 이 파일에 남긴다** — 무엇을 하기 전에 읽어야 할 것들이다.
> 쪼갤 때 `PROGRESS.md`·`CONTRIBUTING.md`의 상호 참조도 함께 고친다.

## 작업 규칙

1. **Phase 단위로 진행한다.** `docs/ROADMAP.md`에 **적힌 순서대로** 진행하고, 완료 시 체크박스를 갱신한다.
   ⚠️ **번호 순서가 아니라 문서에 배치된 순서다.** 두 번 옮겼다 — 측정(5)을 앞으로, Gateway(4)를 뒤로.
2. **큰 Phase는 Step으로 쪼개고, Step마다 멈춘다.** 구현 → 테스트 → 설명 → 확인 후 다음 Step으로.
   Step마다 독립 커밋을 남긴다.
3. **`docs/PROGRESS.md`를 갱신하면서 작업한다.** Step을 끝낼 때마다 목표, 무엇을 왜 했는지,
   어떤 문제를 만났는지, 커밋 해시를 기록한다. 상단의 "현재 위치"도 최신으로 유지한다.
   여러 Step짜리 작업은 대화만으로는 흐름을 놓치기 쉽다 — 파일에 남아야 따라갈 수 있다.
4. **`main`에 직접 커밋하지 않는다.** 브랜치 전략은 `docs/CONTRIBUTING.md` 참고.
   실패하는 테스트(문제 재현용 등)는 `feature/*` 안에서만 존재할 수 있다.
5. **요청받지 않은 푸시를 하지 않는다.** 로컬 git 작업을 되돌릴 때는 `refs/backup/*`을 먼저 남긴다
   — `git checkout --`은 추적 중인 파일의 미커밋 변경을 복구 불가능하게 지운다.
6. **문제를 먼저 재현하고 고친다.** 패턴을 적용하기 전에 현재 코드가 깨지는 걸 실패하는 테스트로
   드러내는 방식을 쓴다 (Phase 2 Step 0 참고).
7. **검증은 테스트 코드로 한다.** 아래 "검증 방법" 참고.
8. **라이브러리·엔진을 도입하면 `docs/DECISIONS.md`에 기록한다.** 양식은 그 문서에 있고,
   **면접 질문 순서 그대로**다 — 문제 / 자체 구현이 가르쳐준 것 / 대안 비교 / 선택 이유 /
   **포기한 것** / 검증 방법. 각 항목에 30초짜리 "면접 한 줄"을 단다.
   **자체 구현의 이유를 지우지 않는다** — 코드는 지우되 `PROGRESS.md`의 서술은 남긴다.
   대비가 이 저장소의 자산이다.
9. **엔티티를 바꾸면 마이그레이션 파일도 함께 쓴다.** (Phase 6부터, `DECISIONS.md` D-001)
   스키마는 `ddl-auto`가 아니라 `*/src/main/resources/db/migration`의 SQL이 만든다.
   `ddl-auto`는 `validate`뿐이라 **빠뜨리면 기동이 실패한다.** 파일 이름은 `V<번호>__<설명>.sql`이고
   **이미 나간 파일은 고치지 않는다** (체크섬이 어긋나 기동이 막힌다). 고칠 게 있으면 다음 번호로 쓴다.
   ⚠️ 원장(ledger)은 MongoDB라 아직 예외다 — `MongoIndexInitializer`가 기동할 때 만든다.
10. **알아야 할 것은 전부 저장소 문서에 적는다.** 에이전트의 개인 메모리나 세션 기록에 두지 않는다.
    사람도 다른 도구도 읽을 수 있어야 하고, 저장소와 함께 버전 관리되어야 한다.
    자리는 위 "문서 지도"에 있다.
11. **커밋·PR 본문에 서사를 풀지 않는다.** 제목 한 줄로 무엇을 했는지 적고 본문은 불릿으로 쓴다.
    양식과 예시는 `docs/CONTRIBUTING.md`의 "커밋 컨벤션"·"PR 작성" 절에 있다.
    ⚠️ **2026-09-11 이전의 `git log`와 PR 본문은 본보기가 아니다** — 그 규칙이 생기기 전 것이다.

## 검증 방법

**동작 확인은 반드시 테스트 코드로 남긴다.** curl이나 수동 실행으로 "잘 되네" 하고 넘어가지 않는다.

수동 검증은 그 순간에만 존재한다. 다음 사람도, 다음 달의 나도, CI도 그걸 다시 해주지 않는다.

```
구현 → 테스트 작성 → 통과 확인 → (필요하면) 수동 e2e로 보강
```

### 수동 실행은 보조 수단

서비스를 띄워 curl로 확인하는 건 **테스트가 놓친 걸 찾는 용도**로 유용하다. Phase 2에서 이렇게 찾은
버그가 여럿이다 (멱등성 키가 삭제되어 이중 출금이 가능했던 문제, Outbox payload가 TINYTEXT로
생성된 문제). 다만 **수동으로 발견했다면 고치는 것으로 끝내지 말고 반드시 테스트로 고정한다.**

### 테스트가 진짜로 잡는지 확인하기

새로 쓴 테스트는 **일부러 코드를 되돌려 실패하는지** 본다. 통과하는 것만 보면 실은 아무것도
검증하지 않는 테스트를 통과로 착각할 수 있다.

Testcontainers MySQL 전환 때 이 방법으로 확인했다 — `@Lob` 길이 지정을 되돌리자 통과하던 테스트
8건이 실패했다. 반대로 시각 정밀도 버그는 되돌려도 로컬에서 통과해서, macOS에서는 재현되지
않는다는 걸 알게 됐다 (아래 "로컬에서 잡히지 않는 것").

### `main` 승격은 테스트만으로 정하지 않는다

**`./gradlew test`가 통과하는 것은 필요조건이지 충분조건이 아니다.** `release/phase-N` → `main` 전에
**모든 서비스를 함께 띄운 크로스 서비스 e2e**를 돌리고 결과를 `PROGRESS.md`에 적는다.

통합 테스트는 이벤트를 **테스트가 직접** Kafka에 넣으므로, 진짜 프로듀서가 그 토픽에 그 모양으로
내는지는 검증하지 못한다. 실제로 테스트가 전부 초록인 채 e2e에서만 잡힌 것들이다.

- 토픽이 파티션 1개로 자동 생성되어 Saga가 5분간 멈춤
- 상태 전이 경합
- 멱등성 키 삭제로 이중 출금 가능
- Outbox payload가 TINYTEXT로 생성

## 저장소 구조

Gradle 멀티모듈. **서비스 간에 공유 라이브러리를 두지 않는다** —
DTO가 중복되더라도 서비스 경계를 유지하는 쪽을 택했다.

| 모듈 | 포트 | 저장소 |
|---|---|---|
| `account-service` | 8081 | MySQL `account_db` (+ Redis 분산 락) |
| `transfer-service` | 8082 | MySQL `transfer_db` |
| `ledger-service` | 8083 | MongoDB `ledger_db` |
| `reconciliation-service` | 8084 | MySQL `reconciliation_db` |
| `notification-service` | 8085 | MySQL `notification_db` |
| `gateway` | 8080 | (Phase 4에서 구현) |
| `config-server` | 8888 | (Phase 4에서 구현) |

`gateway`와 `config-server`는 아직 Phase 0 스켈레톤이다. 특히 `config-server`는 지금 실행해도
즉시 종료된다 — 버그가 아니라 미구현 상태다.

### 구조와 흐름은 `docs/ARCHITECTURE.md`에 있다

**코드를 건드리기 전에 그 문서를 읽는다.** 서비스 간 동기 호출이 없고 각 서비스가 이벤트를 보고
스스로 다음을 발행하는 구조(Choreography Saga)라, **흐름 전체를 한눈에 볼 수 있는 코드가 없다.**

| 건드리는 것 | 볼 절 |
|---|---|
| 이벤트 필드·토픽 | §9 토픽 한눈에 — **여러 서비스에 같은 계약이 중복 정의**되어 있어 함께 고쳐야 한다 |
| 잔액을 바꾸는 코드 | §5 — 모든 경로가 `BalanceJournal`을 지나야 한다. 하나만 빠져도 대사가 무의미해진다 |
| 새 Kafka 리스너 | §7 — 에러 핸들링 설정이 안 걸리면 **메시지가 조용히 사라진다** |
| 컨슈머 로직 | §6 — 서비스마다 중복 수신 대응 방식이 다르다 |
| 대사·`/internal/reconciliation/*` | §8 — **읽기 전용이다.** 대사는 찾아서 알리기만 하고 고치지 않는다 |

## 실행 · 테스트

```bash
# 1. 로컬 인프라 기동 (MySQL·MongoDB·Redis·Kafka. 서비스 컨테이너화는 Phase 6)
docker compose -f docker-compose.dev.yml up -d

# 2. 테스트
./gradlew test

# 3. 서비스 실행 (각각 별도 터미널)
./gradlew :account-service:bootRun
```

- `account-service` / `transfer-service`는 **MySQL이 떠 있지 않으면 부팅 자체가 실패**한다
  (JPA가 시작 시점에 DB에 붙어 dialect를 판별하기 때문).
- Testcontainers를 쓰는 통합 테스트는 Docker 데몬이 필요하다.

> **성능은 노트북에서 재지 않는다.** 부하 생성기와 측정 대상이 같은 CPU를 두고 싸워서,
> "앱이 느린 것"과 "부하 생성기가 앱의 CPU를 뺏은 것"이 구분되지 않는다.
> 측정 전용 홈서버 절차는 `docs/HOMELAB.md`에 있다 (집 안이면 `ssh home1`, 집 밖이면 `ssh home2`).
>
> ⚠️ **재기동 직후 첫 실행은 버린다.** JVM·커넥션 풀·컨슈머 리밸런스가 안 풀린 상태라
> 데워진 실행과 비교하면 없는 개선이 보인다. 2026-08-24에 32.0초 vs 2.1초로 "15.4배"라고 적을
> 뻔했고, 다시 재니 둘 다 2.1초였다. **A/B는 같은 온도에서 잰다.**
>
> ⚠️ **DB 설정도 저장소와 같은지 확인한다.** 실험용으로 손댄 MySQL 설정을 되돌리지 않은 채 잰 값을
> 결과로 적은 적이 있다 — 앱 커밋 해시는 확인했는데 DB는 아무도 확인하지 않았다.
> **서버 설정을 손으로 바꿨으면 되돌리는 것까지가 실험이다.**
> `scripts/measure-hot-account.sh`가 이제 재기 전에 확인하고, 다르면 멈춘다.

### 통합 테스트용 컨테이너

각 서비스의 통합 테스트 베이스를 상속해서 쓴다. Java는 단일 상속이라 **그 서비스가 필요로 하는
컨테이너를 베이스 하나에 모아**둔다.

| 베이스 | 띄우는 컨테이너 |
|---|---|
| `AbstractIntegrationTest` (account) | MySQL + Redis + Kafka |
| `AbstractIntegrationTest` (transfer) | MySQL + Kafka |
| `AbstractIntegrationTest` (ledger) | MongoDB + Kafka |

**인메모리 DB(H2)를 쓰지 않는다.** 운영과 같은 MySQL을 컨테이너로 띄운다. H2로 돌리던 시절,
테스트를 전부 통과하고도 MySQL에서만 터지는 버그가 두 번 있었다 — `@Lob` 컬럼이 TINYTEXT로
만들어져 "Data too long", 그리고 예약어(`key`) 문제다.

> ⚠️ `@Testcontainers` + `@Container` 조합으로 바꾸지 않는다. 그 조합은 **테스트 클래스가 끝날 때마다
> 컨테이너를 멈추기** 때문에, 베이스를 상속한 클래스가 둘 이상이면 두 번째부터 "Unable to connect"로
> 실패한다. (Step 2에서 실제로 겪음) 모든 베이스는 **싱글턴 컨테이너 패턴**이다
> (static 블록에서 한 번 start + `@DynamicPropertySource`).

### 로컬에서 잡히지 않는 것

**시각 정밀도 문제는 macOS에서 재현되지 않는다.** macOS의 `Instant.now()`는 마이크로초까지만 주지만
Linux는 나노초까지 준다. `support/Timestamps`의 잘라내기를 없애도 로컬 테스트는 통과하고
**CI(Linux)에서만 실패한다.** 시각을 다루는 코드를 고쳤다면 로컬 통과만 믿지 말고 CI를 확인한다.

## 환경 주의사항

**Spring Boot 4.1 / Jackson 3 / Testcontainers 2.x** 조합이라 웹에 있는 Boot 3.x 기준 예제와
패키지·아티팩트 좌표가 다른 경우가 많다. 이미 겪은 사례들이 `docs/PROGRESS.md`의
"Spring Boot 4.1 이행 이슈" 표에 있으니 **새 의존성을 추가하기 전에 먼저 확인한다.**

새 라이브러리를 붙일 때는 기억에 의존하지 말고, Boot BOM이 그 의존성을 관리하는지
(`./gradlew :{모듈}:dependencies --configuration testCompileClasspath`) 먼저 확인하는 편이 빠르다.

## 언어

문서·커밋 메시지·테스트 메서드명은 한글로 쓴다. 코드 식별자는 영문이다.
